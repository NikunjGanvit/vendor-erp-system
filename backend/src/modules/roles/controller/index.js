'use strict';

const { createRoleAction, getRoleByIdAction, updateRoleAction, deleteRoleAction, listRolesAction } = require('./roles');

module.exports = {
  createRoleAction,
  getRoleByIdAction,
  updateRoleAction,
  deleteRoleAction,
  listRolesAction,
};
